apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  clusterIP: None
  ports:
  - port: 8787
    targetPort: 8787
  selector:
    app: {{ .Release.Name }}
