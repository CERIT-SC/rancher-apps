# Logging Operator Subsriber

Deploys components (`Output` and `Flow`) that use the **Logging Operator** to monitor the selected application. This Subscriber uses `labels` to select the desired application.
