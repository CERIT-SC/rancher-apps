apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataspecer.fullname" . }}
  labels:
    {{- include "dataspecer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dataspecer.selectorLabels" . | nindent 4 }}
