apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "dataspecer.fullname" . }}-database
  labels:
    {{- include "dataspecer.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
